{
  "name": "sql-dom",
  "version": "0.1.0",
  "private": true,
  "description": "SQL-inspired language for DOM manipulation",
  "scripts": {
    "start": "vite",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -b",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "monaco-editor": "^0.52.2"
  }
}
